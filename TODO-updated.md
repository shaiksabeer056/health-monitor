# TODO-updated

- [ ] Replace `index.html` with the Neural Predictor HTML (currently in `index.html.html`).
- [ ] Confirm `styles.css` and `script.js` are loaded.
- [ ] Run quick sanity check in browser (no missing element errors).

