# TODO

- [ ] Fix entry HTML so UI elements match `script.js` (use Neural Predictor HTML).
- [ ] Ensure `index.html` includes `styles.css` and `script.js`.
- [ ] Verify page loads without JS DOM errors.

